class Solution {
    public ArrayList<Integer> prefSum(int[] arr) {
        // code here
        int temp=0;
        ArrayList<Integer> list=new ArrayList<>();
        for(int num:arr){
            temp+=num;
            list.add(temp);
        }
        return list;
    }
}
