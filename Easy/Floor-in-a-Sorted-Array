class Solution {
    public int findFloor(int[] arr, int x) {
        // code here
        int val=-1,ind=-1;
        for(int i = 0;i<arr.length;i++){
            if(arr[i]<=x){
                ind=i;
            }
        }
        return ind;
    }
}
